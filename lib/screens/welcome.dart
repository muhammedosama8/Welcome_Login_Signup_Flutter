import 'package:flutter/material.dart';
import 'package:welcomeloginsignuppages/screens/body.dart';

class WelcomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(), //body.dart
    );
  }
}
